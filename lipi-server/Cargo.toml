[package]
name = "lipi-server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
uuid = "1"
async-trait = "0.1"

lipi-contracts = { path = "../lipi-contracts" }
